dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 1
  name: bev_rasters_trainval
  cache_dir: /home/saveas/Documents/CS231N-dataset/bev_rasters
  keys_to_load:
    - pixels
    - action
  keys_to_cache:
    - action
